# Supabase — leave unset to run on the built-in local data adapter.
# Create a project at https://supabase.com, run supabase/schema.sql in the
# SQL editor, then fill these in.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
